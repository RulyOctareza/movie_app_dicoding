name: movie_app_dicoding
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.7
  http: ^1.4.0
  shared_preferences: ^2.5.3
  flutter_bloc: ^9.1.1
  flutter_dotenv: ^5.2.1
  matcher: ^0.12.17
  firebase_core: ^3.15.1
  firebase_analytics: ^11.5.2
  firebase_crashlytics: ^4.3.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mockito: ^5.4.6
  build_runner: ^2.5.4
  bloc_test: ^10.0.0
  network_image_mock: ^2.1.1

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/certificates/
    - .env
